/*
use plugin_handler::Plugin;

struct Session {
    local: bool,
    view_plugins: Vec<Rc<Plugin>>,
    backend: Option<Rc<Plugin>>,
}

impl Session {
    fn new() -> Session {
        Session {
            local: false,
            view_plugins: Vec::new(),
            backend: None,
        }
    }

    pub fn start_local(_: &str,  _: Option<&str>) {

    }

    pub fn start_remate(_: &str, _: usize) {

    }
}
*/
